// Pull in Cloudflare Workers runtime types (D1Database, ExecutionContext,
// Fetcher, ScheduledController, etc.) used across the API routes, cron, and
// worker entry. Kept as a reference so it merges with Next's DOM lib.
/// <reference types="@cloudflare/workers-types" />
